# Cloudflare Pages _redirects for React SPA
# Bot detection and SEO HTML serving is handled by functions/_middleware.js

# Handle pretty URLs for about page  
/about  /learn-about-tepa.html  200

# SPA fallback - serves index.html for all unmatched routes
# Static assets are automatically served by Cloudflare before this rule
/*  /index.html  200